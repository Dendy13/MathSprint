# MathSprint API Routes
# Semua endpoint FastAPI didefinisikan di sini.
# Route handler hanya melakukan: validasi input → panggil core engine → return response.
# Tidak boleh ada business logic di route handler.
